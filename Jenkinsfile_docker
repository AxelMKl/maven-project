pipeline {
	agent any
	stages{
		stage('Build'){
			steps{
				sh 'mvn clean package'
				sh 'docker build . -t tomcatwebapp:1'
				sh 'docker run -d -p 192.168.50.30:9090:8080 tomcatwebapp:1'
			}
		}
	}
}