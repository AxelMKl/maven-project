node {
    stage('Build'){
        sh 'bash ./gradlew clean build'
        }
        post {
            success {
                echo 'Now Archiving...'
                archiveArtifacts artifacts: '**/target/*.war'
            }
        }
    }
}